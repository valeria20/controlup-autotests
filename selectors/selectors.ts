// the enums below are data-test values
export enum LoginPageSelectors {
    usernameInput = "username",
    passwordInput = "password",
    loginButton = "login-button",
}

export enum InventoryPageSelectors {
  items = "inventory-item", // data-test value
  addToCartButton = "//button[contains(@class, 'btn_inventory')]",
  cartBadge = "shopping-cart-badge", // data-test value
}